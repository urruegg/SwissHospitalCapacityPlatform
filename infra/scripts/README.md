# Infra Scripts

Sprint 03 baseline is workflow-first.

No imperative deployment scripts are required at this stage because GitHub Actions workflows under `.github/workflows/` run Bicep build, what-if, and deployment.
