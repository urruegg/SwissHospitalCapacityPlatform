@prefix rdf:    <http://www.w3.org/1999/02/22-rdf-syntax-ns#> .
@prefix rdfs:   <http://www.w3.org/2000/01/rdf-schema#> .
@prefix owl:    <http://www.w3.org/2002/07/owl#> .
@prefix xsd:    <http://www.w3.org/2001/XMLSchema#> .
@prefix dcterms: <http://purl.org/dc/terms/> .
@prefix skos:   <http://www.w3.org/2004/02/skos/core#> .

# ---------------------------------------------------------------------------
# Published imports (references only; not axiomatically loaded in this skeleton).
# Sprint 09 scope: declare + label so crosswalk.md can reference them; full
# axiomatic import + reasoning is Phase 3 per AMA §9.3 L-06.
# ---------------------------------------------------------------------------
@prefix bfo:    <http://purl.obolibrary.org/obo/BFO_> .
@prefix omrse:  <http://purl.obolibrary.org/obo/OMRSE_> .
@prefix ogms:   <http://purl.obolibrary.org/obo/OGMS_> .
@prefix oostt:  <http://purl.obolibrary.org/obo/OOSTT_> .
@prefix iao:    <http://purl.obolibrary.org/obo/IAO_> .
@prefix obo:    <http://purl.obolibrary.org/obo/> .

# ---------------------------------------------------------------------------
# Platform-specific namespace (Swiss Hospital Capacity Platform).
# ---------------------------------------------------------------------------
@prefix hcp:    <https://swisshcapacity.example/ontology/hcp#> .

# ===========================================================================
# Ontology header
# ===========================================================================
<https://swisshcapacity.example/ontology/hcp> a owl:Ontology ;
    dcterms:title       "Swiss Hospital Capacity Platform — Reference Ontology (skeleton)"@en ;
    dcterms:description "Reference layer of the two-layer ontology per ADR-0014. Skeleton only — full axiomatic content is Phase 3."@en ;
    dcterms:creator     "Urs Rüegg"@en ;
    dcterms:issued      "2026-07-02"^^xsd:date ;
    owl:versionInfo     "0.5.0" ;
    rdfs:comment        "Sprint 09 RB-11 deliverable; Sprint 23 (#255) adds the Curavias organisation spine + skills ontology block; Sprint 26 WS-A (#335) adds the Foresight tier (hcp:Forecast/Driver + forWard/explainedBy/evidencedBy); Sprint 26 WS-B (#335) adds the Decision tier hcp:Barrier + barrierForWard. See docs/ontology/README.md for scope discipline and versioning."@en ;

    # Imports declared but not loaded axiomatically in this skeleton.
    # A follow-up PR (or an ontology-authoring tool) resolves these to full IRIs.
    owl:imports
        <http://purl.obolibrary.org/obo/bfo.owl> ,      # BFO (ISO/IEC 21838-2:2021)
        <http://purl.obolibrary.org/obo/omrse.owl> ,    # OMRSE — health-care facility function typology
        <http://purl.obolibrary.org/obo/ogms.owl> ,     # OGMS — clinical-encounter terms
        <http://purl.obolibrary.org/obo/oostt.owl> ,    # OOSTT — organisational structure of care sites
        <http://purl.obolibrary.org/obo/iao.owl> .      # IAO — Information Artifact Ontology

# ===========================================================================
# hcp:CapacityUnit — the platform's integral abstraction (per ADR-0014 §1).
# ===========================================================================
# A CapacityUnit is a material entity (or time-bounded slot) that bears a
# capacity function and has a capacity state. Beds, OR slots, rooms, staff
# shifts and devices are all subtypes. This gives one set of relations,
# states, KPIs, forecasts and simulation logic across all resource
# dimensions.
#
# Note: aligning CapacityUnit under bfo:BFO_0000030 (Object) vs
# bfo:BFO_0000035 (Process boundary) vs a slot-oriented class is a
# Phase 3 decision. Sprint 09 skeleton keeps it as an owl:Class only.

hcp:CapacityUnit a owl:Class ;
    rdfs:label   "Capacity Unit"@en ;
    rdfs:comment "A material entity or time-bounded slot that bears a capacity function and has a capacity state (available / occupied / blocked / planned). Abstract; every resource dimension in scope of the HCC is a subtype."@en ;
    skos:example "Bed, ORSlot, Room, StaffShift, Device."@en .

# --- Subtypes (RB-11 skeleton set) ---

hcp:Bed a owl:Class ;
    rdfs:subClassOf hcp:CapacityUnit ;
    rdfs:label      "Bed"@en ;
    rdfs:comment    "A physical bed as a capacity unit within a ward. First time-series binding target in Sprint 09 (bed-state changes)."@en .

hcp:ORSlot a owl:Class ;
    rdfs:subClassOf hcp:CapacityUnit ;
    rdfs:label      "OR Slot"@en ;
    rdfs:comment    "A time-bounded slot in an operating theatre. Distinct from the OR room itself; the OR steering anchor per ADR-0014 §3 MVO scope."@en .

hcp:Room a owl:Class ;
    rdfs:subClassOf hcp:CapacityUnit ;
    rdfs:label      "Room"@en ;
    rdfs:comment    "A physical room capacity unit (examination, therapy, consult). Ontological placement under omrse:facility-part-of pending Phase 3 review."@en .

hcp:StaffShift a owl:Class ;
    rdfs:subClassOf hcp:CapacityUnit ;
    rdfs:label      "Staff Shift"@en ;
    rdfs:comment    "A time-bounded shift as a capacity unit. Distinct from the health-care worker (which realises the role). Feeds the future FR-STAFF-* family (RB-10 unrelated; SBA agent per AMA F-A-06)."@en .

hcp:Device a owl:Class ;
    rdfs:subClassOf hcp:CapacityUnit ;
    rdfs:label      "Device"@en ;
    rdfs:comment    "A biomedical device treated as a capacity unit (availability + PM windows + monitoring state). Extends dim_device per AMA §5.4."@en .

# ===========================================================================
# hcp:CapacityState — the shared state vocabulary across every CapacityUnit.
# ===========================================================================
hcp:CapacityState a owl:Class ;
    rdfs:label   "Capacity State"@en ;
    rdfs:comment "The instantaneous state of a CapacityUnit: available / occupied / blocked / planned. Bound to time-series data via the operational layer."@en .

hcp:Available a hcp:CapacityState ;
    rdfs:label "available"@en .

hcp:Occupied a hcp:CapacityState ;
    rdfs:label "occupied"@en .

hcp:Blocked a hcp:CapacityState ;
    rdfs:label "blocked"@en ;
    rdfs:comment "Unit is offline (maintenance, isolation, cleaning). Reason recorded via a data property in the operational layer."@en .

hcp:Planned a hcp:CapacityState ;
    rdfs:label "planned"@en ;
    rdfs:comment "Unit is scheduled for future use (typical for ORSlot; also used for bed pre-assignment)."@en .

# ===========================================================================
# Skeleton class stubs for range targets (declared here to avoid dangling
# references; full axiomatic content is Phase 3 per ADR-0014 §3).
# ===========================================================================

hcp:Encounter a owl:Class ;
    rdfs:label   "Encounter"@en ;
    rdfs:comment "A hospitalisation episode (control unit per AMA SD Core Solution Pattern). Aligns to ogms:Encounter in Phase 3. Range target for hcp:appliesTo, hcp:assignsEncounter; crosswalked to DC-DEMAND-ENCOUNTER-v1 in T1.4."@en .

hcp:Specialty a owl:Class ;
    rdfs:label   "Specialty"@en ;
    rdfs:comment "A medical specialty (FMH discipline). Aligns to omrse:Specialty in Phase 3. Range target for hcp:covers; source table is dim_specialty (DC-MASTER-02)."@en .

# ===========================================================================
# Basic relations (skeleton). Full property chains + inverse properties are
# Phase 3 material.
# ===========================================================================
hcp:hasState a owl:ObjectProperty ;
    rdfs:label   "has state"@en ;
    rdfs:domain  hcp:CapacityUnit ;
    rdfs:range   hcp:CapacityState .

hcp:isPartOf a owl:ObjectProperty ;
    rdfs:label   "is part of"@en ;
    rdfs:comment "Compositional relation. Bed ⊑ Room ⊑ Ward ⊑ Hospital. Subject to Phase 3 alignment with bfo:part-of."@en .

# ===========================================================================
# Information Content Entity abstract root (aligns to iao:InformationContentEntity in Phase 3)
# ===========================================================================
hcp:InformationContent a owl:Class ;
    rdfs:label   "Information Content"@en ;
    rdfs:comment "Abstract superclass for scores, forecasts, recommendations, and matching outputs. Aligns to iao:InformationContentEntity in Phase 3."@en .

# --- Four new ICE classes ---

hcp:BedAssignment a owl:Class ;
    rdfs:subClassOf hcp:InformationContent ;
    rdfs:label      "Bed Assignment"@en ;
    rdfs:comment    "The matching output linking one Encounter to one Bed for a time window. Carries assignedAt, unassignedAt, assignmentReason, matchScore, explanationTokens. Grounds AMA SD Core Solution Pattern 'Matching Demand -> Supply'."@en .

hcp:DischargeReadinessScore a owl:Class ;
    rdfs:subClassOf hcp:InformationContent ;
    rdfs:label      "Discharge Readiness Score"@en ;
    rdfs:comment    "A score [0..1] produced by the discharge-scoring pipeline with feature attribution. Grounds FR-DC-001, FR-DC-006."@en .

hcp:DischargeRecommendation a owl:Class ;
    rdfs:subClassOf hcp:InformationContent ;
    rdfs:label      "Discharge Recommendation"@en ;
    rdfs:comment    "Ranked candidate action derived from a DischargeReadinessScore, including blockers and recommendedAction. Advisory/HITL per NFR-AI-001. Grounds FR-DC-002, FR-DC-003, FR-DC-005."@en .

hcp:ForecastOutput a owl:Class ;
    rdfs:subClassOf hcp:InformationContent ;
    rdfs:label      "Forecast Output"@en ;
    rdfs:comment    "A 72h forecast covering one Specialty for a time window. Carries covers, validFor, refreshCadence, producedBy. Grounds FR-FC-001..006."@en .

# ===========================================================================
# Seven new object/datatype properties for the ICE classes
# ===========================================================================
hcp:appliesTo a owl:ObjectProperty ;
    rdfs:label  "applies to"@en ;
    rdfs:domain hcp:InformationContent ;
    rdfs:range  hcp:Encounter .

hcp:assignsBed a owl:ObjectProperty ;
    rdfs:label  "assigns bed"@en ;
    rdfs:domain hcp:BedAssignment ;
    rdfs:range  hcp:Bed .

hcp:assignsEncounter a owl:ObjectProperty ;
    rdfs:label  "assigns encounter"@en ;
    rdfs:domain hcp:BedAssignment ;
    rdfs:range  hcp:Encounter .

hcp:covers a owl:ObjectProperty ;
    rdfs:label  "covers"@en ;
    rdfs:domain hcp:ForecastOutput ;
    rdfs:range  hcp:Specialty .

hcp:validFor a owl:DatatypeProperty ;
    rdfs:label  "valid for (time window)"@en ;
    rdfs:domain hcp:ForecastOutput ;
    rdfs:range  xsd:duration .

hcp:producedBy a owl:DatatypeProperty ;
    rdfs:label  "produced by (model run id)"@en ;
    rdfs:domain hcp:InformationContent ;
    rdfs:range  xsd:string .

hcp:hasExplanation a owl:DatatypeProperty ;
    rdfs:label  "has explanation tokens"@en ;
    rdfs:domain hcp:InformationContent ;
    rdfs:range  xsd:string .

# ===========================================================================
# Trusted External Signals ontology extension (Sprint 21 M4)
# ===========================================================================

hcp:TrustedSource a owl:Class ;
    rdfs:label   "Trusted Source"@en ;
    rdfs:comment "An authoritative organisation or feed source for trusted external hazard signals. Anchored to OOSTT organisation patterns and IAO information-source semantics; operational Fabric IQ binding is GA-gated per ADR-0014."@en .

hcp:HazardType a owl:Class ;
    rdfs:label   "Hazard Type"@en ;
    rdfs:comment "A governed taxonomy entry for a public hazard such as heat, flood, earthquake, epidemic surge, or civil-protection alert. Anchored to environmental-hazard terminology; represented by the DC-EXT-SIGNAL-v1 hazardType field."@en .

hcp:ExternalSignal a owl:Class ;
    rdfs:subClassOf hcp:InformationContent ;
    rdfs:label      "External Signal"@en ;
    rdfs:comment    "A CAP-aligned public warning or authority signal ingested from a TrustedSource. Anchored to IAO information content entity; dual-bound to Eventhouse and Lakehouse history for advisory CSA trigger evaluation."@en .

hcp:HazardEvent a owl:Class ;
    rdfs:label   "Hazard Event"@en ;
    rdfs:comment "A real-world hazardous occurrent inferred or deduplicated from one or more ExternalSignal records. Anchored to BFO occurrent; used as the event-level object handed to CSA."@en .

hcp:TriggerRule a owl:Class ;
    rdfs:subClassOf hcp:InformationContent ;
    rdfs:label      "Trigger Rule"@en ;
    rdfs:comment    "A directive information entity that maps qualifying external signals or hazard events to advisory ScenarioTemplate and LageTier pre-seeds. Anchored to IAO directive information entity; operational rules live in trigger_rules.yaml."@en .

hcp:AffectedRegion a owl:Class ;
    rdfs:subClassOf hcp:Location ;
    rdfs:label      "Affected Region"@en ;
    rdfs:comment    "A geographic Location affected by an ExternalSignal or HazardEvent, represented by canton, NUTS, or polygon fields in DC-EXT-SIGNAL-v1. Reuses the existing Location concept for the reference-layer region block."@en .

hcp:signalFromSource a owl:ObjectProperty ;
    rdfs:label   "signal from source"@en ;
    rdfs:comment "Links an ExternalSignal to the TrustedSource that published or originated it."@en ;
    rdfs:domain  hcp:ExternalSignal ;
    rdfs:range   hcp:TrustedSource .

hcp:signalIndicatesHazard a owl:ObjectProperty ;
    rdfs:label   "signal indicates hazard"@en ;
    rdfs:comment "Links an ExternalSignal to the HazardType or HazardEvent it indicates after normalization and deduplication."@en ;
    rdfs:domain  hcp:ExternalSignal ;
    rdfs:range   hcp:HazardType .

hcp:signalAffectsRegion a owl:ObjectProperty ;
    rdfs:label   "signal affects region"@en ;
    rdfs:comment "Links an ExternalSignal to the AffectedRegion in its normalized region block."@en ;
    rdfs:domain  hcp:ExternalSignal ;
    rdfs:range   hcp:AffectedRegion .

hcp:triggerRuleMapsScenario a owl:ObjectProperty ;
    rdfs:label   "trigger rule maps scenario"@en ;
    rdfs:comment "Links a TriggerRule to the ScenarioTemplate that should be pre-seeded when the rule qualifies."@en ;
    rdfs:domain  hcp:TriggerRule ;
    rdfs:range   hcp:ScenarioTemplate .

hcp:signalPreseeds a owl:ObjectProperty ;
    rdfs:label   "signal pre-seeds"@en ;
    rdfs:comment "Links an ExternalSignal to the LageTier that it proposes as an advisory pre-seed; the CSA classifier remains authoritative."@en ;
    rdfs:domain  hcp:ExternalSignal ;
    rdfs:range   hcp:LageTier .

# ===========================================================================
# Foresight tier (Sprint 26 WS-A, issue #335, design spec §3.2).
# Turns the descriptive occupancy surface into the predictive tier: a
# ward-level occupancy Forecast, its Driver decomposition ('why'), and the
# evidencing link to the existing Trusted-External-Signal spine. hcp:Signal is
# intentionally NOT introduced — hcp:ExternalSignal is reused (univocity).
# ===========================================================================

hcp:Ward a owl:Class ;
    rdfs:label   "Ward"@en ;
    rdfs:comment "A hospital ward as a capacity-bearing organisational unit (bed rollup). Range target for hcp:forWard; source table is dim_ward_capacityunit (DC-MASTER-07). Full placement under omrse:hospital-part-of is Phase 3."@en .

hcp:Forecast a owl:Class ;
    rdfs:subClassOf hcp:InformationContent ;
    rdfs:label      "Forecast"@en ;
    rdfs:comment    "A 72h occupancy forecast for one Ward across hourly horizons (0..72h), carrying forecast occupied beds / occupancy % and a capacity-breach flag. Distinct from hcp:ForecastOutput (specialty demand); bound to gold.fact_occupancy_forecast (DC-OCCUPANCY-FORECAST-v1). Deterministic synthetic generator today with a clean seam for a real model (design D2)."@en .

hcp:Driver a owl:Class ;
    rdfs:subClassOf hcp:InformationContent ;
    rdfs:label      "Driver"@en ;
    rdfs:comment    "A decomposition factor explaining a Forecast (forecast_admissions / planned_discharges / transfers / seasonality) with a signed bed delta. Driver deltas reconcile to the net forecast change. Bound to gold.fact_forecast_driver (DC-FORECAST-DRIVER-v1). Grounds beat 2 ('Warum') of the actionable-insight pattern."@en .

hcp:forWard a owl:ObjectProperty ;
    rdfs:label  "for ward"@en ;
    rdfs:domain hcp:Forecast ;
    rdfs:range  hcp:Ward .

hcp:explainedBy a owl:ObjectProperty ;
    rdfs:label  "explained by"@en ;
    rdfs:domain hcp:Forecast ;
    rdfs:range  hcp:Driver .

hcp:evidencedBy a owl:ObjectProperty ;
    rdfs:label   "evidenced by"@en ;
    rdfs:comment "Links a Driver to the ExternalSignal that evidences it (e.g. a seasonality driver evidenced by a BAG epidemic signal). Reuses the Sprint 21 signal spine; advisory only."@en ;
    rdfs:domain  hcp:Driver ;
    rdfs:range   hcp:ExternalSignal .

hcp:Barrier a owl:Class ;
    rdfs:subClassOf hcp:InformationContent ;
    rdfs:label      "Barrier"@en ;
    rdfs:comment    "A systemic discharge barrier: the deterministic collapse of N discharge-blocked candidates into one ranked, owner-attributed obstacle (barrier_type, owner_role, candidate_count, bed_impact, aged_h, clears_at, spanning wards). The DCA 'N candidates collapse into M barriers' pattern. Runtime-derived by the pure derive_barriers builder and materialised to gold.fact_discharge_barrier (DC-DISCHARGE-BARRIER-v1). No PHI: aggregate over opaque candidate keys + ward IDs. Grounds beat 3 (the actionable barrier) of the actionable-insight pattern for the DCA role."@en .

hcp:barrierForWard a owl:ObjectProperty ;
    rdfs:label   "barrier for ward"@en ;
    rdfs:comment "Links a Barrier to each Ward it spans (a barrier may span several wards once its candidates are collapsed by barrier_type)."@en ;
    rdfs:domain  hcp:Barrier ;
    rdfs:range   hcp:Ward .

# ===========================================================================
# Curavias organisation spine + skills ontology extension (Sprint 23, #255)
# ===========================================================================
# Realises design D6 "reuse verbatim, extend don't replace": the org spine
# folds dim_hospital into the Curavias tenants 1:1 (T6) and the skills domain
# is expressed on two orthogonal axes -- Proficiency (1..5, how capable) and
# Assurance (L0..L4, how proven; drives eligibility gating). The care-setting
# split (T14) separates bed/nursing demand from ops/doctor demand. Operational
# Fabric IQ bindings are GA-gated per ADR-0014; crosswalk.md records each row.

# --- Organisation spine (aligns to OOSTT organisational structure) ---

hcp:Tenant a owl:Class ;
    rdfs:label   "Tenant"@en ;
    rdfs:comment "A Curavias healthcare-provider organisation (CuraNova / Curalp / Vialta). Folded 1:1 into the operational Hospital dimension per Sprint 23 T6 (tenant_id = hospital_id). Anchored to OOSTT organisation patterns."@en .

hcp:OrgUnit a owl:Class ;
    rdfs:label   "Organisational Unit"@en ;
    rdfs:comment "A site or organisational unit within a Tenant (e.g. a campus or building). Anchored to OOSTT organisational-part semantics."@en .

hcp:Department a owl:Class ;
    rdfs:label   "Department"@en ;
    rdfs:comment "A clinical department (Klinik / Fachbereich) within an OrgUnit; the demand and gap grain for skills. Anchored to OOSTT administrative-part semantics."@en .

hcp:CareSetting a owl:Class ;
    rdfs:label   "Care Setting"@en ;
    rdfs:comment "The care-setting split (Sprint 23 T14): 'bed' = nursing / Pflegepersonal demand; 'ops' = doctors + specialised (OR / anaesthesia) demand. Categorises SkillDemand and SkillGap so nursing and ops shortages are answered separately."@en .

# --- Skills domain (ESCO / ISCO-08 aligned) ---

hcp:Skill a owl:Class ;
    rdfs:label   "Skill"@en ;
    rdfs:comment "A demonstrable competency held by a HealthWorker and required by a WorkforcePosition; ESCO-aligned. Reused verbatim from the Curavias skills ontology (D6)."@en .

hcp:OccupationRole a owl:Class ;
    rdfs:label   "Occupation Role"@en ;
    rdfs:comment "The occupation held by a HealthWorker (e.g. registered nurse, anaesthetist); ISCO-08 aligned. Reused verbatim from the Curavias skills ontology (D6)."@en .

hcp:HealthWorker a owl:Class ;
    rdfs:label   "Health Worker"@en ;
    rdfs:comment "A person bearing an occupation role, identified by GLN -- the golden-thread worker-identity key validated (mod-10) at the silver gate. Reused verbatim from the Curavias skills ontology (D6). No PHI is carried in the operational binding."@en .

hcp:SkillAssertion a owl:Class ;
    rdfs:subClassOf hcp:InformationContent ;
    rdfs:label      "Skill Assertion"@en ;
    rdfs:comment    "An evidence-backed statement that a HealthWorker holds a Skill at a given Proficiency (1..5) and Assurance (L0..L4) for a validity period. Carries hasProficiency, hasAssurance, assertsSkill, assertedByWorker. Grounds Sprint 23 T7."@en .

hcp:SkillDemand a owl:Class ;
    rdfs:subClassOf hcp:InformationContent ;
    rdfs:label      "Skill Demand"@en ;
    rdfs:comment    "A required Skill at a minimum Proficiency and minimum Assurance for a Department and CareSetting over a time window. Carries demandsSkill, demandForCareSetting, hasProficiency, hasAssurance. Grounds Sprint 23 T7/T14."@en .

hcp:SkillGap a owl:Class ;
    rdfs:subClassOf hcp:InformationContent ;
    rdfs:label      "Skill Gap"@en ;
    rdfs:comment    "The shortfall between SkillDemand and valid assurance-gated supply for a Skill, Department, CareSetting and time window. Carries gapForCareSetting. Grounds the skills gap measures (PR #339) and Sprint 23 T14."@en .

hcp:WorkerUnitEligibility a owl:Class ;
    rdfs:subClassOf hcp:InformationContent ;
    rdfs:label      "Worker Unit Eligibility"@en ;
    rdfs:comment    "A pre-computed statement that a HealthWorker may safely staff a CapacityUnit, gated on assurance-valid SkillAssertions against the unit's RoleSkillTemplate. Carries eligibleForUnit. Grounds Sprint 23 T7."@en .

hcp:RoleSkillTemplate a owl:Class ;
    rdfs:subClassOf hcp:InformationContent ;
    rdfs:label      "Role Skill Template"@en ;
    rdfs:comment    "The reusable template mapping an OccupationRole (and specialisation) to its required Skills, minimum Proficiency and minimum Assurance. Drives SkillDemand generation and eligibility checks. Reused verbatim from the Curavias skills ontology (D6)."@en .

# --- Org spine + skills object / datatype properties ---

hcp:orgUnitOfTenant a owl:ObjectProperty ;
    rdfs:label  "org unit of tenant"@en ;
    rdfs:domain hcp:OrgUnit ;
    rdfs:range  hcp:Tenant .

hcp:departmentOfOrgUnit a owl:ObjectProperty ;
    rdfs:label  "department of org unit"@en ;
    rdfs:domain hcp:Department ;
    rdfs:range  hcp:OrgUnit .

hcp:hasOccupationRole a owl:ObjectProperty ;
    rdfs:label  "has occupation role"@en ;
    rdfs:domain hcp:HealthWorker ;
    rdfs:range  hcp:OccupationRole .

hcp:assertsSkill a owl:ObjectProperty ;
    rdfs:label  "asserts skill"@en ;
    rdfs:domain hcp:SkillAssertion ;
    rdfs:range  hcp:Skill .

hcp:assertedByWorker a owl:ObjectProperty ;
    rdfs:label  "asserted by worker"@en ;
    rdfs:domain hcp:SkillAssertion ;
    rdfs:range  hcp:HealthWorker .

hcp:demandsSkill a owl:ObjectProperty ;
    rdfs:label  "demands skill"@en ;
    rdfs:domain hcp:SkillDemand ;
    rdfs:range  hcp:Skill .

hcp:demandForCareSetting a owl:ObjectProperty ;
    rdfs:label  "demand for care setting"@en ;
    rdfs:domain hcp:SkillDemand ;
    rdfs:range  hcp:CareSetting .

hcp:gapForCareSetting a owl:ObjectProperty ;
    rdfs:label  "gap for care setting"@en ;
    rdfs:domain hcp:SkillGap ;
    rdfs:range  hcp:CareSetting .

hcp:roleRequiresSkill a owl:ObjectProperty ;
    rdfs:label  "role requires skill"@en ;
    rdfs:domain hcp:RoleSkillTemplate ;
    rdfs:range  hcp:Skill .

hcp:eligibleForUnit a owl:ObjectProperty ;
    rdfs:label  "eligible for unit"@en ;
    rdfs:domain hcp:WorkerUnitEligibility ;
    rdfs:range  hcp:CapacityUnit .

hcp:workerGln a owl:DatatypeProperty ;
    rdfs:label   "worker GLN (golden-thread identity key)"@en ;
    rdfs:comment "The GLN worker-identity key, mod-10 validated at the silver gate; the golden thread across assertions, eligibility and demand."@en ;
    rdfs:domain  hcp:HealthWorker ;
    rdfs:range   xsd:string .

hcp:hasProficiency a owl:DatatypeProperty ;
    rdfs:label   "has proficiency (1..5, how capable)"@en ;
    rdfs:comment "The Proficiency axis: 1 = novice .. 5 = expert. Orthogonal to Assurance. Applies to SkillAssertion (attained) and SkillDemand (minimum required)."@en ;
    rdfs:domain  hcp:InformationContent ;
    rdfs:range   xsd:integer .

hcp:hasAssurance a owl:DatatypeProperty ;
    rdfs:label   "has assurance (L0..L4, how proven)"@en ;
    rdfs:comment "The Assurance axis: L0 = self-declared .. L4 = federally registered. Orthogonal to Proficiency and drives eligibility gating. Applies to SkillAssertion (attained) and SkillDemand (minimum required)."@en ;
    rdfs:domain  hcp:InformationContent ;
    rdfs:range   xsd:string .

# ===========================================================================
# Provider-extension pattern (per FR-ONT-007).
# Providers add subclasses under their own namespace, importing this
# skeleton. Example (informal, not axiomatic in this file):
#
#   @prefix uzh: <https://usz.example/ontology/> .
#   uzh:ICUBed rdfs:subClassOf hcp:Bed .
#
# The crosswalk (docs/ontology/crosswalk.md) records each provider
# extension against Fabric IQ operational entities.
# ===========================================================================
