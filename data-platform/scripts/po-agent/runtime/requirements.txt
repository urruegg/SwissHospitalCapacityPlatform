fastapi>=0.115,<1
uvicorn[standard]>=0.32,<1
pydantic>=2.9,<3
# The Dockerfile also copies the corpus/liveproof/cost/ontology sibling
# modules into this same image (get_tools() imports them) - their real deps
# (Sprint 42 fix: previously missing entirely, so every class's
# DefaultAzureCredential import silently failed and get_tools() registered
# zero tools, no matter what the env vars/RBAC said).
azure-identity>=1.17.0,<2
requests>=2.32,<3
pyyaml>=6.0,<7
