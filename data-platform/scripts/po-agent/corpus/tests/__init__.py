"""WS-A Class A corpus pipeline tests (Sprint 28, issue #377).

Package marker so the sibling modules (snapshot, chunk_tag, phi_gate, publish)
resolve on sys.path when pytest collects this directory.
"""
